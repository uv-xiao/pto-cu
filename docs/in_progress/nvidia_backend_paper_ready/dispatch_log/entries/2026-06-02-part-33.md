# 2026-06-02 Part 33 Dispatch Entries

## Paper Baseline Probe Script Split

- Scope:
  - Replaced the long `paper_baseline_probe.py` implementation body with a
    stable CLI and compatibility export module.
  - Moved default paths, JSON I/O, shell command helpers, probe checks, and
    final raw-probe artifact assembly into focused modules under
    `paper_baseline_probe_impl/`.
  - Preserved the original helper names used by the script path for future
    refresh tooling.
- Why:
  - Keeps the source/dependency probe gate reviewable before MPK, VDCores,
    vLLM, SGLang, and ThunderKittens baseline execution work spends GPU time.
- Verification:
  - Passed Python compile checks, CLI probe generation to `tmp/`, focused
    paper-baseline probe pytest, diff check, changelog validation, and
    aggregate NVIDIA review guard validation for this slice.
