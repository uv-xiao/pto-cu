# 2026-06-01 Part 14 Dispatch Entries

### 2026-06-01 - Decode Loop Unit Math Bridge

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned
  decode-loop runner bridge for repeated Qwen unit-math live execution.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode mock --run-unit-math-live
  --device 0 --arch compute_80 --repeat-runs 3 --output-json
  tmp/cuda-backend/pto-serving-decode-loop-unit-math-bridge-2026-06-01/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  decode-loop readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, in-progress evaluation docs,
  changelog docs, benchmark-viewer data, dispatch log, and focused tests.
  No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: this bridge executes a hidden-size-4
  unit-math diagnostic DAG, not the full Qwen/Qwen3-8B serving graph.
- Verification commands and results: the runner artifact reported
  `unit_math_live_bridge_contract.status=diagnostic_bridge_executed`,
  `repeat_runs=3`, `total_completed_count=12`, `total_error_count=0`, and
  `max_abs_error=0.0`.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the decode-loop runner entry point now
  owns an executable CUDA bridge summary. Remaining PTO full-serving gaps are
  numerically correct full Qwen kernels, full `cuda_live` decode-loop
  execution, and full-serving viewer-result import.

### 2026-06-01 - Decode Loop Token Owner Live

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned runner
  integration for the CUDA-live token pointer-table owner.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode mock --token-cuda-live
  --device 0 --output-json
  tmp/cuda-backend/pto-serving-decode-loop-token-live-2026-06-01/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  decode-loop readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, in-progress evaluation docs,
  changelog docs, benchmark-viewer data, dispatch log, and focused tests.
  No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: this opens only the token pointer
  table in CUDA-live mode; KV-cache and resident weights remain dry-run.
- Verification commands and results: the runner artifact reported
  `mode=partial_cuda_live_submission_plan`,
  `cuda_live_resource_owners=["token_pointer_table"]`, and
  `resource_lifecycle_modes.token_pointer_table=cuda_live`.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the decode-loop runner now owns the
  token CUDA buffers in the same artifact that orders persistent DAG
  submission. Remaining PTO full-serving gaps are CUDA-live KV-cache and
  resident weight ownership, numerically correct Qwen kernels, full
  `cuda_live` decode-loop execution, and full-serving viewer-result import.
