# 2026-06-02 Part 67

## Tensor Import Smoke Record Guard

- Required imported tensor smoke viewer rows to match benchmark id, hardware,
  compute target, tensor shape, raw artifact, correctness, and sample count.
- Added A100 `compute_80` metadata to the Qwen attention and MLP import-smoke
  records so the validator can reject stale or cross-machine imports.
