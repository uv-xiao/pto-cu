# 2026-06-02 Part 25 Dispatch Entries

## Benchmark Viewer JavaScript Split

- Scope:
  - Split `docs/nvidia-backend/benchmark-viewer/viewer.js` into a small
    entrypoint plus focused ES modules under
    `docs/nvidia-backend/benchmark-viewer/viewer/`.
  - Updated `index.html` to load the viewer entrypoint as a module.
  - Fixed the viewer data loader to populate `paperBaselineEnvironmentPlans`,
    which the baseline-readiness view already rendered.
  - Updated NVIDIA review guards and artifact tests to scan the whole viewer
    module tree for rendered contract fields.
- Why:
  - Keeps the benchmark-status viewer human-reviewable while preserving its
    JSON-backed review contract for benchmarks, methods, paper baselines,
    serving workloads, goal progress, work queue, paper matrix, and results.
- Verification:
  - Passed JavaScript syntax checks for the entrypoint and all viewer modules.
  - Passed focused viewer data validation, aggregate NVIDIA review guard, and
    targeted review-artifact pytest for the viewer contract.
