# 2026-06-02 Part 20 Dispatch Entries

## Benchmark Viewer Validator Split

- Scope:
  - Replaced the 2,015-line benchmark-viewer validation script body with a
    stable wrapper at `.agents/checks/validate_benchmark_viewer_data.py`.
  - Moved the validator implementation into focused modules under
    `.agents/checks/benchmark_viewer_validation/`.
  - Preserved the existing command path used by review guards and tests.
- Why:
  - Makes the evidence guard itself human-reviewable while keeping the same
    benchmark-viewer data contract.
- Verification:
  - `validate_benchmark_viewer_data.py` passed after the split.
