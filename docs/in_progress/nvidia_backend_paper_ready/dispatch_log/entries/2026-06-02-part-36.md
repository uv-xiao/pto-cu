# 2026-06-02 Part 36 Dispatch Entries

## Paper Serving Command Plan Script Split

- Scope:
  - Replaced the long `paper_serving_command_plan.py` implementation body with
    a stable CLI and compatibility export shim.
  - Moved plan assembly, per-baseline command builders, JSON I/O, path helpers,
    record identity helpers, shell rendering, and Git metadata lookup into
    focused modules under `paper_serving_command_plan_impl/`.
  - Preserved the public helper names used by tests and benchmark-viewer
    command-plan documentation.
- Why:
  - Keeps the paper-baseline serving command planner reviewable while
    preserving the generated `serving_command_plans` contract.
- Verification:
  - Passed Python compile checks, focused serving-command-plan pytest, diff
    check, changelog validation, and aggregate NVIDIA review guard validation
    for this slice.
