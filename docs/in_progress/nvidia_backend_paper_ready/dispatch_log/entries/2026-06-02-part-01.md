# 2026-06-02 Part 01 Dispatch Entries

### 2026-06-02 - Decode Loop KV Owner Live

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned runner
  integration for the CUDA-live KV-cache owner.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode mock --token-cuda-live
  --kv-cuda-live --device 0 --output-json
  tmp/cuda-backend/pto-serving-decode-loop-token-kv-live-2026-06-02/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  decode-loop readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, in-progress evaluation docs,
  changelog docs, benchmark-viewer data, dispatch log, and focused tests.
  No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: this opens token and KV-cache owners
  in CUDA-live mode; resident weights remain dry-run and Qwen kernels are not
  executed.
- Verification commands and results: the standalone KV artifact reported
  `mode=cuda_live`, `pointer_count=20`, `freed_pointer_count=20`, and
  `total_byte_count=5851054080`. The runner artifact reported
  `cuda_live_resource_owners=["token_pointer_table","kv_cache"]`.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the decode-loop runner now owns token
  and KV CUDA buffers in the same artifact that orders persistent DAG
  submission. Remaining PTO full-serving gaps are CUDA-live resident weight
  ownership in the runner, numerically correct Qwen kernels, full
  `cuda_live` decode-loop execution, and full-serving viewer-result import.

### 2026-06-02 - Decode Loop Resident Owner Live

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned runner
  integration for the CUDA-live resident weight-table owner.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode mock --token-cuda-live
  --kv-cuda-live --resident-cuda-live --device 0 --output-json
  tmp/cuda-backend/pto-serving-decode-loop-token-kv-resident-live-2026-06-02/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  decode-loop readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, in-progress evaluation docs,
  changelog docs, benchmark-viewer data, dispatch log, and focused tests.
  No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: this opens token, KV-cache, and
  resident-weight owners in CUDA-live mode; Qwen kernels are still not
  executed.
- Verification commands and results: the runner artifact reported
  `cuda_live_resource_owners=["token_pointer_table","kv_cache",`
  `"resident_weight_table"]`,
  `resource_lifecycle_modes.resident_weight_table=cuda_live`,
  `resident_weight_pointer_count=399`, and
  `resident_weight_task_count=255`.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the decode-loop runner now opens all
  planned Qwen serving resource owners in CUDA-live mode. Remaining PTO
  full-serving gaps are numerically correct Qwen kernels, full `cuda_live`
  decode-loop execution, and full-serving viewer-result import.

### 2026-06-02 - Decode Loop Submission Descriptors

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned descriptor
  bridge from live resource owners to Qwen persistent task functions.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode mock --token-cuda-live
  --kv-cuda-live --resident-cuda-live --device 0 --output-json
  tmp/cuda-backend/pto-serving-decode-loop-submission-descriptors-2026-06-02/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  decode-loop readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, in-progress evaluation docs,
  changelog docs, benchmark-viewer data, dispatch log, and focused tests.
  No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: descriptors are resource-backed and
  ready for `cuda/persistent_device`, but full Qwen `run_prepared` execution
  is still not performed.
- Verification commands and results: the artifact reported
  `resource_backed_descriptors_ready`, `execution_status=not_executed`,
  `mpk_offline_decode` with 255 graph tasks and 1024 planned repetitions, and
  `vdcores_offline_decode` with 255 graph tasks and 64 planned repetitions.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the runner now exports the concrete
  function-id and resource-field submission contract. Remaining gaps are
  executing those descriptors, validating Qwen numerical correctness, and
  importing full-serving viewer rows.

### 2026-06-02 - Diagnostic Submission Smoke

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned smoke for
  the Qwen descriptor task-function set.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode mock
  --run-submission-smoke --token-cuda-live --kv-cuda-live
  --resident-cuda-live --device 0 --arch compute_80 --output-json
  tmp/cuda-backend/pto-serving-decode-loop-submission-smoke-live-2026-06-02/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  decode-loop readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, in-progress evaluation docs,
  changelog docs, benchmark-viewer data, dispatch log, and focused tests.
  No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: the smoke uses controlled buffers and
  proves the function-id set can execute; it is not full Qwen serving.
- Verification commands and results: the artifact reported
  `resource_backed_descriptors_ready`,
  `execution_status=diagnostic_descriptor_smoke_passed`,
  `completed_count=10`, `error_count=0`, `device_wall_ns=84992`, and
  `max_abs_error<=1e-5`.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the runner can attach diagnostic
  `run_prepared` evidence for function ids 7100 through 7109. Remaining gaps
  are resource-backed full decode-loop execution, Qwen numerical correctness,
  and full-serving viewer import.

### 2026-06-02 - Descriptor Smoke Audit Refresh

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned refresh of
  paper-readiness data after descriptor-smoke import.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  .agents/skills/cuda-backend-eval/scripts/refresh_nvidia_review_artifacts.py`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, keeping the paper audit
  aligned with current CUDA evidence.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: benchmark-viewer matrix/audit/work queue,
  changelog docs, and dispatch log. No upstream repositories were edited or
  pushed.
- Dependencies and blocked assumptions: this is evidence alignment only; it
  does not reduce the full-serving paper blockers.
- Verification commands and results: `validate_benchmark_viewer_data.py`,
  `validate_nvidia_changelog.py`, `check_nvidia_review_ready.py`, and the
  focused review-artifact pytest selector all passed.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: LLM-serving evidence counts now include
  the descriptor-smoke raw artifact and viewer row. Remaining blockers are PTO
  resource-backed full-serving rows, VDCores full-serving correctness, and
  ThunderKittens-family full-serving rows.

### 2026-06-02 - Resource-Backed Graph Materialization

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned bridge from
  live resource owners to concrete Qwen DAG task descriptors.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode mock
  --run-submission-smoke --token-cuda-live --kv-cuda-live
  --resident-cuda-live --device 0 --arch compute_80 --output-json
  tmp/cuda-backend/pto-serving-resource-backed-graph-2026-06-02/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO resource-backed
  decode-loop readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer matrix,
  changelog docs, dispatch log, and focused tests. No upstream repositories
  were edited or pushed.
- Dependencies and blocked assumptions: graph materialization is still not
  full decode-loop execution or Qwen numerical correctness.
- Verification commands and results: Qwen decode-loop pytest, focused
  review-artifact regeneration pytest, `validate_cuda_examples.py`,
  `validate_benchmark_viewer_data.py`, `validate_nvidia_changelog.py`,
  `check_nvidia_review_ready.py`, `py_compile`, and `git diff --check` all
  passed.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the runner now proves both serving
  policies can bind 255 graph tasks, 399 resident weight pointers, three
  token fields, and two KV fields to CUDA-live pointers. Remaining gap is
  executing that resource-backed graph through `run_prepared`.
