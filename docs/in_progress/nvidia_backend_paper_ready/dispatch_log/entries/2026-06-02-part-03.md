# 2026-06-02 Part 03 Dispatch Entries

### 2026-06-02 - Paper Work Queue Evidence Summary

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned
  paper-readiness reviewability improvement.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  .agents/skills/cuda-backend-eval/scripts/refresh_nvidia_review_artifacts.py`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, human-reviewable
  benchmark status and strict document/data quality.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: benchmark-viewer data, review-artifact scripts,
  viewer UI, changelog docs, dispatch log, and focused tests. No upstream
  repositories were edited or pushed.
- Dependencies and blocked assumptions: this is a reviewability/data-contract
  change. It does not complete PTO, VDCores, or ThunderKittens full-serving
  paper rows.
- Verification commands and results:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  .agents/checks/validate_benchmark_viewer_data.py` passed, and the focused
  paper-readiness pytest selectors passed.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the PTO full-serving work item now has a
  concise action plus structured evidence bullets. Remaining paper-readiness
  blockers are numerically correct PTO Qwen full-serving execution, VDCores
  Qwen3-8B full serving, and ThunderKittens-family full-serving rows.

### 2026-06-02 - Qwen Resource-Backed Full Logits Check

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned PTO
  resource-backed diagnostic logits coverage improvement.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode offline
  --single-context-live-session --run-resource-backed-smoke
  --resource-backed-repeat-runs 3 --device 0 --arch compute_80
  --output-json tmp/cuda-backend/pto-serving-resource-backed-full-logits-check-2026-06-02/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO persistent-device
  Qwen serving evidence for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data, in-progress
  evaluation docs, changelog docs, dispatch log, and focused tests. No
  upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: this remains diagnostic execution.
  It proves all written diagnostic logits elements, not full Qwen numerical
  correctness.
- Verification commands and results: both `mpk_offline_decode` and
  `vdcores_offline_decode` completed three resource-backed `run_prepared`
  submissions, 765 completed tasks, zero scheduler errors, 2,430,976 logits
  elements written, and 2,430,976 diagnostic logits checks with max absolute
  error `0.0`.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the diagnostic logits evidence now
  covers the full written buffer for both serving policies. Remaining PTO gaps
  are numerically correct Qwen task bodies, full token-by-token decode-loop
  execution, and full-serving viewer import.

### 2026-06-02 - Qwen Resource-Backed Decode-Step Execution

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned PTO
  bounded decode-step execution evidence.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode offline
  --single-context-live-session --run-resource-backed-smoke
  --resource-backed-decode-steps 2 --device 0 --arch compute_80
  --output-json tmp/cuda-backend/pto-serving-resource-backed-decode-steps-2026-06-02/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO persistent-device
  Qwen serving evidence for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data, changelog
  docs, dispatch log, and focused tests. No upstream repositories were edited
  or pushed.
- Dependencies and blocked assumptions: this remains diagnostic execution.
  It proves bounded decode-step submission using diagnostic task bodies, not
  full Qwen numerical correctness.
- Verification commands and results: `mpk_offline_decode` and
  `vdcores_offline_decode` each executed 2 bounded diagnostic decode steps,
  completed 510 tasks, hit zero scheduler errors, and checked all 2,430,976
  written diagnostic logits elements with zero mismatches.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the PTO resource-backed path now reports
  planned versus executed decode-step counts. Remaining PTO gaps are
  numerically correct Qwen task bodies, full token-by-token decode-loop
  execution, and full-serving viewer import.

### 2026-06-02 - Qwen Resource-Backed Token Feedback

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned PTO
  diagnostic sampled-token feedback evidence.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode offline
  --single-context-live-session --run-resource-backed-smoke
  --resource-backed-decode-steps 2 --device 0 --arch compute_80
  --output-json tmp/cuda-backend/pto-serving-resource-backed-token-feedback-2026-06-02/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO persistent-device
  Qwen serving evidence for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data, changelog
  docs, dispatch log, and one focused unit test. No upstream repositories were
  edited or pushed.
- Dependencies and blocked assumptions: this remains diagnostic execution.
  Token feedback is committed by the host between prepared submissions, so it
  does not prove device-side sampling or full Qwen numerical correctness.
- Verification commands and results: `mpk_offline_decode` and
  `vdcores_offline_decode` each executed 2 bounded diagnostic decode steps,
  completed 510 tasks, hit zero scheduler errors, committed 2 sampled tokens
  through `output_ids` and `input_ids`, and checked all 2,430,976 written
  diagnostic logits elements with zero mismatches.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the PTO resource-backed path now records
  sampled-token movement across bounded diagnostic decode steps. Remaining PTO
  gaps are numerically correct Qwen task bodies, device-side token feedback,
  full token-by-token decode-loop execution, and full-serving viewer import.

### 2026-06-02 - Qwen Device Token Feedback

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned PTO
  device-side diagnostic token feedback evidence.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode offline
  --single-context-live-session --run-resource-backed-smoke
  --resource-backed-decode-steps 2 --device 0 --arch compute_80
  --output-json tmp/cuda-backend/pto-serving-resource-backed-device-feedback-2026-06-02/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO persistent-device
  Qwen serving evidence for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data, changelog
  docs, dispatch log, and focused tests. No upstream repositories were edited
  or pushed.
- Dependencies and blocked assumptions: this remains diagnostic execution.
  The device task writes sampled-token feedback, but the task bodies are still
  diagnostic formulas rather than numerically correct Qwen kernels.
- Verification commands and results: `mpk_offline_decode` and
  `vdcores_offline_decode` each executed 2 bounded diagnostic decode steps,
  completed 510 tasks, hit zero scheduler errors, observed 2 device-committed
  sampled tokens through `output_ids` and `input_ids`, and checked all
  2,430,976 written diagnostic logits elements with zero mismatches.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the PTO resource-backed path now proves
  device-side sampled-token feedback for bounded diagnostic decode steps.
  Remaining PTO gaps are numerically correct Qwen task bodies, full
  token-by-token decode-loop execution, and full-serving viewer import.

### 2026-06-02 - Qwen VDCores Full Diagnostic Decode

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned longer
  diagnostic decode-loop evidence for the VDCores serving policy.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode offline
  --single-context-live-session --run-resource-backed-smoke
  --resource-backed-workload vdcores_offline_decode
  --resource-backed-decode-steps 64
  --resource-backed-logits-check-policy final_step --device 0
  --arch compute_80 --output-json $ARTIFACT/qwen-decode-loop-runner.json`,
  where `$ARTIFACT` is
  `tmp/cuda-backend/pto-serving-resource-backed-vdcores-full-decode-2026-06-02`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO persistent-device
  Qwen serving evidence for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data, changelog
  docs, dispatch log, and focused tests. No upstream repositories were edited
  or pushed.
- Dependencies and blocked assumptions: this remains diagnostic execution.
  The longer loop proves scheduler, resource, and device-feedback mechanics,
  but not numerically correct Qwen kernels.
- Verification commands and results: `vdcores_offline_decode` executed 64
  bounded diagnostic decode steps, completed 16,320 tasks, hit zero scheduler
  errors, observed 64 device-side token-feedback commits, deferred 63 logits
  checks, and checked all 2,430,976 diagnostic logits elements on the final
  step with zero mismatches.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the VDCores diagnostic policy now has a
  full planned 64-step decode-loop artifact with final-step logits checking.
  Remaining PTO gaps are numerically correct Qwen task bodies, MPK longer-loop
  parity, and full-serving viewer import.
