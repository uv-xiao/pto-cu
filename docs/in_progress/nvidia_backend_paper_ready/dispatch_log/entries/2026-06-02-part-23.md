# 2026-06-02 Part 23 Dispatch Entries

## NVIDIA Review Guard Split

- Scope:
  - Replaced the long `.agents/checks/check_nvidia_review_ready.py` body with
    a stable wrapper.
  - Moved shared helpers, goal/docs checks, viewer-data checks, external
    contract checks, and policy checks into focused modules under
    `.agents/checks/nvidia_review_guard/`.
  - Preserved the existing review guard command and output.
- Why:
  - Keeps the top-level NVIDIA evidence guard itself human-reviewable while
    preserving the repository-wide readiness contract.
- Verification:
  - `check_nvidia_review_ready.py` passed after the split.
