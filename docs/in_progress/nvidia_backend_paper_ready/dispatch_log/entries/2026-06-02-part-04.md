# 2026-06-02 Part 04 Dispatch Entries

### 2026-06-02 - Qwen VDCores Unit Numeric Branches

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned partial
  numeric-mode bridge for resource-backed PTO Qwen execution.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode offline
  --single-context-live-session --run-resource-backed-smoke
  --resource-backed-workload vdcores_offline_decode
  --resource-backed-decode-steps 64
  --resource-backed-logits-check-policy final_step
  --resource-backed-numeric-task-mode unit_math --device 0
  --arch compute_80 --output-json $ARTIFACT/qwen-decode-loop-runner.json`,
  where `$ARTIFACT` is
  `tmp/cuda-backend/pto-serving-resource-backed-vdcores-unit-numeric-2026-06-02`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO persistent-device
  Qwen serving evidence for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data, changelog
  docs, dispatch log, and focused tests. No upstream repositories were edited
  or pushed.
- Dependencies and blocked assumptions: this remains partial numeric-mode
  execution. RMSNorm is intentionally not enabled for full resource-backed
  hidden buffers; the current RMSNorm unit-math branch is covered by the small
  unit-math live DAG.
- Verification commands and results: `vdcores_offline_decode` executed all 64
  planned diagnostic decode steps, completed 16,320 tasks, hit zero scheduler
  errors, observed 64 device-side token-feedback commits, and checked all
  2,430,976 diagnostic logits elements on the final step with zero mismatches.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: resource-backed execution can now opt
  into safe O(n) unit-math branches for `qwen_attention_qkv` and
  `qwen_mlp_gate_up`. Remaining PTO gaps are full hidden-size numeric kernels,
  full-serving decode execution, and paper-ready full-serving viewer rows.

### 2026-06-02 - VDCores Segmented Window Manifest

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned conversion
  of the VDCores shared-instruction capacity blocker into a bounded window
  manifest.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  .agents/skills/cuda-backend-eval/scripts/vdcores_instruction_window_plan.py`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, VDCores Qwen3-8B
  full-serving baseline readiness.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA evaluation scripts, benchmark-viewer execution
  attempt metadata, changelog docs, dispatch log, and one focused unit test.
  No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: this is still not a runnable VDCores
  baseline row. It turns the measured 512-instruction shared-table limit into
  5 compute windows and 30 memory windows per SM for the next builder/runtime
  implementation step.
- Verification commands and results: the planner regenerated
  `$ARTIFACT/shared-window-plan.json` with largest compute and memory windows
  of 512 instructions and a final memory window of 194 instructions, where
  `$ARTIFACT` is
  `tmp/cuda-backend/paper-baselines/vdcores/qwen3-8b-shared-window-plan-0a0392d2`.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: implement the segmented shared-window
  builder/runtime path against the manifest or fix the global-instruction
  correctness regression, then rerun Qwen3-8B `-N 64 -b 5` before importing a
  VDCores paper-serving row.
