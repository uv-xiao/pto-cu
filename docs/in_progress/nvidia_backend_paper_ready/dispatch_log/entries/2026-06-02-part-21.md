# 2026-06-02 Part 21 Dispatch Entries

## Qwen CUDA Weight Binding Split

- Scope:
  - Replaced the long `examples/cuda/qwen_cuda_weight_binding.py` body with a
    stable CLI and compatibility wrapper.
  - Moved shared constants, safetensors binding planning, bounded CUDA copy,
    full-residency CUDA probing, and artifact construction into focused
    modules under `examples/cuda/qwen_cuda_weight_binding_impl/`.
  - Updated the CUDA example validator to scan split `_impl` packages for
    evidence symbols.
- Why:
  - Keeps the Qwen weight-binding evidence path human-reviewable while
    preserving existing script commands and spec-loaded imports.
- Verification:
  - `qwen_cuda_weight_binding.py --no-cuda-probe` passed.
  - Focused pytest `persistent_qwen_cuda_weight_binding_is_reviewable` passed.
  - `check_nvidia_review_ready.py` passed after the split.
