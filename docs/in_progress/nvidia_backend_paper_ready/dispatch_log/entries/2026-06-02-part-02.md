# 2026-06-02 Part 02 Dispatch Entries

### 2026-06-02 - VDCores Shared-Instruction Window Plan

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned VDCores
  Qwen3-8B shared-runtime blocker narrowing.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  .agents/skills/cuda-backend-eval/scripts/vdcores_instruction_window_plan.py`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, VDCores full-serving
  evidence for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: benchmark-viewer data, VDCores evaluation tooling,
  in-progress evaluation docs, changelog docs, dispatch log, and focused
  tests. No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: this is analysis-only evidence from
  the existing H200 capacity capture. It does not make the VDCores result
  importable.
- Verification commands and results: the generated raw artifact shows the
  512-instruction shared table needs at least 5 compute-instruction windows
  and 30 memory-instruction windows per SM for Qwen3-8B decode64.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the VDCores paper row remains blocked
  until either the global-instruction correctness regression is fixed or a
  segmented/token-windowed shared-instruction schedule executes with
  correctness passing.

### 2026-06-02 - Qwen Resource-Backed Repeat Execution

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned PTO Qwen
  resource-backed repeat execution evidence.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode mock
  --single-context-live-session --run-resource-backed-smoke
  --resource-backed-repeat-runs 3 --token-cuda-live --kv-cuda-live
  --resident-cuda-live --workspace-cuda-live --device 0 --arch compute_80
  --output-json tmp/cuda-backend/pto-serving-resource-backed-repeat-2026-06-02/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  decode-loop readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data, in-progress
  evaluation docs, changelog docs, dispatch log, and focused tests. No
  upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: this remains diagnostic execution
  with diagnostic task bodies, not full Qwen numerical correctness.
- Verification commands and results: both `mpk_offline_decode` and
  `vdcores_offline_decode` completed three resource-backed `run_prepared`
  submissions, 765 completed tasks, and zero scheduler errors.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: prepared-callable reuse now has
  resource-backed Qwen DAG evidence. Remaining PTO gaps are numerically
  correct Qwen task bodies, full token-by-token decode-loop execution, and
  full-serving viewer import.

### 2026-06-02 - Qwen Resource-Backed Logits Sampling

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned PTO Qwen
  resource-backed output-evidence narrowing.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode offline
  --single-context-live-session --run-resource-backed-smoke
  --resource-backed-repeat-runs 3 --device 0 --arch compute_80
  --output-json tmp/cuda-backend/pto-serving-resource-backed-logits-sampling-2026-06-02/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  decode-loop readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data, in-progress
  evaluation docs, changelog docs, dispatch log, and focused tests. No
  upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: this remains diagnostic execution.
  The logits summary samples only the written prefix, not full-vocabulary Qwen
  logits.
- Verification commands and results: both `mpk_offline_decode` and
  `vdcores_offline_decode` completed three resource-backed `run_prepared`
  submissions, 765 completed tasks, zero scheduler errors, and stable top
  token id 0 over the sampled written logits prefix.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: output evidence now records partial
  logits coverage explicitly. Remaining PTO gaps are full-vocabulary logits,
  numerically correct Qwen task bodies, full token-by-token decode-loop
  execution, and full-serving viewer import.

### 2026-06-02 - Qwen Resource-Backed Full Logits Write

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned PTO Qwen
  resource-backed output-coverage narrowing.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode offline
  --single-context-live-session --run-resource-backed-smoke
  --resource-backed-repeat-runs 3 --device 0 --arch compute_80
  --output-json tmp/cuda-backend/pto-serving-resource-backed-full-logits-write-2026-06-02/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  decode-loop readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data, in-progress
  evaluation docs, changelog docs, dispatch log, and focused tests. No
  upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: this remains diagnostic execution.
  The logits buffer is fully written, but the values are not validated against
  Qwen reference logits.
- Verification commands and results: both `mpk_offline_decode` and
  `vdcores_offline_decode` completed three resource-backed `run_prepared`
  submissions, 765 completed tasks, zero scheduler errors, and
  2,430,976-element logits-buffer write coverage with bounded prefix sampling.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the previous partial-write limitation is
  removed from the diagnostic path. Remaining PTO gaps are numerically correct
  Qwen task bodies, reference-checked logits, full token-by-token decode-loop
  execution, and full-serving viewer import.
