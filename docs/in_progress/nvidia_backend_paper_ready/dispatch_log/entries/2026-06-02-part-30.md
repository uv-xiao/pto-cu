# 2026-06-02 Part 30 Dispatch Entries

## CUDA Example Manifest Split

- Scope:
  - Replaced the long `examples/cuda/manifest.json` body with a stable
    `example_files` index.
  - Moved CUDA example records into focused shards under
    `examples/cuda/manifest/` for core examples, Qwen lifecycle examples,
    Qwen token/runtime examples, and Qwen weight examples.
  - Updated `.agents/checks/validate_cuda_examples.py` to load both the old
    inline manifest and the new sharded manifest contract.
- Why:
  - Keeps the CUDA example contract human-reviewable while preserving the
    stable manifest path required by review guards and example validation.
- Verification:
  - Passed focused CUDA example validation, changelog validation, diff check,
    aggregate NVIDIA review guard validation, and focused artifact pytest for
    this slice.
