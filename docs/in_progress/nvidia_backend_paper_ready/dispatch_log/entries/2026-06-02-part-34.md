# 2026-06-02 Part 34 Dispatch Entries

## Viewer Data I/O Split

- Scope:
  - Replaced the long `viewer_data_io.py` implementation body with a stable
    import-compatibility shim.
  - Moved sharded collection constants, record naming, sidecar read/write
    helpers, logical collection reads, and logical collection writes into
    focused modules under `viewer_data_io_impl/`.
  - Preserved `load_json` and `write_json` imports used by benchmark-viewer
    exporters, refresh scripts, and paper-baseline tooling.
- Why:
  - Keeps the shared sharded-data utility reviewable while preserving the
    logical JSON path contract used by the benchmark viewer.
- Verification:
  - Passed Python compile checks, benchmark-viewer data validation, focused
    sharded-viewer pytest, diff check, changelog validation, and aggregate
    NVIDIA review guard validation for this slice.
