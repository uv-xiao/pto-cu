# Dispatch Log 2026-06-01 Part 13

Entries 73-79 for 2026-06-01.

### 2026-06-01 - Qwen Proxy Decode Loop Live Reuse

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned live CUDA
  evidence update for repeated controlled Qwen proxy decode submissions.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_persistent_microdecode_live.py --device 0
  --arch compute_80 --repeat-runs 3 --output-json
  tmp/cuda-backend/pto-serving-decode-loop-live-2026-06-01/qwen-microdecode-loop.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  implementation readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data,
  in-progress evaluation docs, changelog docs, dispatch log, and focused
  tests. No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: the run reuses one prepared callable
  across three proxy decode submissions and carries mutable KV state, but the
  bodies remain controlled proxy arithmetic rather than full Qwen kernels.
- Verification commands and results: focused TDD test first failed because
  `build_live_microdecode_plan` did not accept `repeat_runs`. After
  implementation the local A100 artifact reported `status=pass`,
  `max_abs_error=0.0`, `total_completed_count=9`, and `total_error_count=0`.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the branch now proves repeated live
  persistent-device submissions through one prepared proxy callable. Remaining
  PTO full-serving gaps are numerically correct Qwen kernels, full
  decode-loop execution, and viewer-result import.

### 2026-06-01 - Dispatch Log Archive Split

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned
  documentation maintainability slice for human review.
- Exact Codex command or script invocation:
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/python -m pytest
  tests/ut/py/test_nvidia_dispatch_log_structure.py -q`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, strict document-quality
  and reviewability guard for the NVIDIA ultimate goal.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: in-progress goal docs, review guard checks,
  focused tests, and changelog docs. No upstream repositories were edited or
  pushed.
- Dependencies and blocked assumptions: the split preserves existing dispatch
  entry text and changes only the review structure.
- Verification commands and results: the focused structure test first failed
  because `dispatch_log/index.md` did not exist, then passed after the archive
  split.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the dispatcher log now has a short
  landing page plus dated archive chunks under 300 lines. Remaining goal gaps
  are the paper-ready serving blockers in the generated work queue.

### 2026-06-01 - Qwen Proxy Decode Loop Viewer Import

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned viewer
  import for the repeated Qwen proxy decode-loop artifact.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  .agents/skills/cuda-backend-eval/scripts/pto_qwen_microdecode_viewer_import.py
  tmp/cuda-backend/pto-serving-decode-loop-live-2026-06-01/qwen-microdecode-loop.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, paper-readiness viewer
  import path for PTO full-serving diagnostics.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA evaluation scripts, benchmark-viewer data,
  in-progress evaluation docs, changelog docs, dispatch log, and focused
  tests. No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: the imported row is explicitly
  `diagnostic_microdecode`, so it must not satisfy full-serving evidence.
- Verification commands and results: focused TDD first failed because the
  viewer row was absent and preflight had no full-serving row filter. After
  implementation, the focused microdecode viewer tests passed.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the viewer now exposes the repeated
  Qwen proxy decode-loop result while keeping full-serving PTO rows blocked
  until numerically correct Qwen kernels and full decode-loop execution exist.

### 2026-06-01 - Qwen Decode Loop Live Bridge

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned Qwen
  decode-loop bridge evidence for human review.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_decode_loop_runner.py --mode offline --output-json
  tmp/cuda-backend/pto-serving-decode-loop-bridge-2026-06-01/qwen-decode-loop-runner.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  implementation readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, preflight checks, benchmark-viewer
  data, in-progress evaluation docs, changelog docs, dispatch log, and
  focused tests. No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: the bridge maps resource-owner fields
  into the controlled proxy live runner only; it does not claim full Qwen
  serving coverage.
- Verification commands and results: focused TDD first failed because
  `cuda_live_bridge_contract` was absent. After implementation,
  `tests/ut/py/test_nvidia_qwen_microdecode_loop.py` passed with 5 tests.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the decode-loop runner now records the
  diagnostic bridge from `a`, `b`, `out`, `c`, `d`, and `tensor_args` into
  the repeated CUDA microdecode runner. Remaining PTO full-serving gaps are
  numerically correct Qwen kernels, full Qwen decode-loop execution, and
  full-serving viewer-result import.

### 2026-06-01 - Qwen Unit Math Oracle

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned Qwen
  task-body math contract for the PTO full-serving path.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_persistent_task_bodies.py --output-json
  tmp/cuda-backend/pto-serving-task-bodies-qwen-unit-2026-06-01/qwen-persistent-task-bodies.json
  --output-source
  tmp/cuda-backend/pto-serving-task-bodies-qwen-unit-2026-06-01/qwen-persistent-task-bodies.cu`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  implementation readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data,
  in-progress evaluation docs, changelog docs, dispatch log, and focused
  tests. No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: the oracle is a hidden-size-4 unit
  reference for Qwen equations; generated CUDA bodies still need to match it
  before this can satisfy kernel correctness.
- Verification commands and results: focused TDD first failed because
  `qwen_unit_math_oracle` was absent. After implementation,
  `tests/ut/py/test_nvidia_qwen_task_body_math.py` passed.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the branch now records reviewable Qwen
  unit equations for RMSNorm, projection, single-token attention cache
  writeback, SiLU/SwiGLU, and logits. Remaining PTO full-serving gaps are CUDA
  task bodies matching that oracle, full Qwen decode-loop execution, and
  full-serving viewer-result import.

### 2026-06-01 - Qwen Unit Math Source Coverage

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned generated
  CUDA source coverage for the Qwen unit math contract.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_persistent_task_bodies.py --output-json
  tmp/cuda-backend/pto-serving-task-bodies-qwen-unit-2026-06-01/qwen-persistent-task-bodies.json
  --output-source
  tmp/cuda-backend/pto-serving-task-bodies-qwen-unit-2026-06-01/qwen-persistent-task-bodies.cu`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  implementation readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data,
  in-progress evaluation docs, changelog docs, dispatch log, and focused
  tests. No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: the generated source now contains an
  opt-in unit-math path; the path still needs live CUDA execution before it
  can replace the proxy diagnostics.
- Verification commands and results: focused TDD first failed because the
  rendered source lacked RMSNorm, cache-writeback, SiLU/SwiGLU, and logits
  unit-math markers. After implementation,
  `tests/ut/py/test_nvidia_qwen_task_body_math.py` passed with 2 tests.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the generated source now carries Qwen
  unit math source coverage. Remaining PTO full-serving gaps are live
  execution of the unit-math path, full Qwen decode-loop execution, and
  full-serving viewer-result import.

### 2026-06-01 - Qwen Unit Math Live Execution

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned live CUDA
  execution evidence for the Qwen unit math path.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_unit_math_live.py --device 0 --arch compute_80
  --output-json
  tmp/cuda-backend/pto-serving-unit-math-live-2026-06-01/qwen-unit-math-live.json`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  implementation readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, benchmark-viewer data,
  in-progress evaluation docs, changelog docs, dispatch log, and focused
  tests. No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: the artifact executes a hidden-size-4
  diagnostic DAG only; it does not claim full Qwen decode-loop coverage.
- Verification commands and results: focused TDD first failed because
  `examples/cuda/qwen_unit_math_live.py` did not exist. After implementation,
  the local A100 artifact reported `status=pass`, `completed_count=4`,
  `error_count=0`, and `max_abs_error=0.0`.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the Qwen unit math source path now has
  live CUDA execution evidence. Remaining PTO full-serving gaps are full Qwen
  decode-loop execution and full-serving viewer-result import.

### 2026-06-01 - Qwen Unit Math Viewer Import

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned import of
  the live Qwen unit-math artifact into the benchmark viewer.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  .agents/skills/cuda-backend-eval/scripts/pto_qwen_unit_math_viewer_import.py
  tmp/cuda-backend/pto-serving-unit-math-live-2026-06-01/qwen-unit-math-live.json`,
  followed by
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  .agents/skills/cuda-backend-eval/scripts/refresh_nvidia_review_artifacts.py`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, human-reviewable PTO
  serving evidence for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA evaluation scripts, benchmark-viewer data,
  in-progress evaluation docs, changelog docs, dispatch log, and focused
  tests. No upstream repositories were edited or pushed.
- Dependencies and blocked assumptions: this row is
  `diagnostic_unit_math`; it must not satisfy the full-serving gate.
- Verification commands and results: focused TDD first failed because
  `pto_qwen_unit_math_viewer_import.py` was absent. After implementation,
  `tests/ut/py/test_nvidia_qwen_unit_math_live.py::test_unit_math_live_importer_marks_result_as_diagnostic`
  passed.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the benchmark viewer now exposes the
  A100 unit-math evidence as a diagnostic PTO row. Remaining PTO
  full-serving gaps are full Qwen decode-loop execution and full-serving
  viewer-result import.

### 2026-06-01 - Qwen Unit Math Prepared Reuse

- Dispatcher session or PR: local Codex session on
  `goal/nvidia-paper-ready`; PR targets `uv-xiao/pto-cu:main`.
- Worker id and objective: no worker dispatched; dispatcher-owned repeated
  live execution for the Qwen unit-math diagnostic DAG.
- Exact Codex command or script invocation:
  `PYTHONPATH=$PWD:$PWD/python .venv/bin/python
  examples/cuda/qwen_unit_math_live.py --device 0 --arch compute_80
  --repeat-runs 3 --output-json
  tmp/cuda-backend/pto-serving-unit-math-live-2026-06-01/qwen-unit-math-live.json`,
  followed by the unit-math viewer importer and
  `refresh_nvidia_review_artifacts.py`.
- Parent goal and child slice:
  `docs/in_progress/nvidia_backend_paper_ready.md`, PTO full-serving
  decode-loop readiness for the LLM-serving paper claim.
- Branch name and PR URL: `goal/nvidia-paper-ready`,
  `https://github.com/uv-xiao/pto-cu/pull/1`.
- Allowed scope and files: CUDA examples, CUDA evaluation scripts,
  benchmark-viewer data, in-progress evaluation docs, changelog docs,
  dispatch log, and focused tests. No upstream repositories were edited or
  pushed.
- Dependencies and blocked assumptions: this is still a hidden-size-4
  diagnostic row, not a full Qwen/Qwen3-8B serving row.
- Verification commands and results: local A100 artifact reported
  `status=pass`, `repeat_runs=3`, `total_completed_count=12`,
  `total_error_count=0`, and `max_abs_error=0.0`.
- Merge decision and merge commit: pending.
- Handoff summary and remaining gaps: the unit-math diagnostic now proves
  prepared persistent callable reuse across three submissions. Remaining PTO
  full-serving gaps are full Qwen decode-loop execution and full-serving
  viewer-result import.
