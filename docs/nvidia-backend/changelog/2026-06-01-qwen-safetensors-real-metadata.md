# 2026-06-01 Qwen Safetensors Real Metadata

## Code And Data Changed

- Downloaded the five real Qwen/Qwen3-8B safetensors shards under
  `tmp/sources/qwen3-8b-safetensors/` using resumable `curl -L -C -`
  commands generated by `examples/cuda/qwen_safetensors_fetch.py`.
- Regenerated shard-status evidence at
  `tmp/cuda-backend/pto-serving-shards-a16851f6/qwen-safetensors-shards.json`.
- Regenerated real metadata evidence at
  `tmp/cuda-backend/pto-serving-safetensors-a16851f6/qwen-safetensors-metadata.json`.
- Updated the Qwen serving scaffold, preflight artifact, benchmark viewer data,
  tests, and in-progress evaluation docs so the current gap is CUDA binding,
  not shard placement or safetensors metadata validation.

## Architecture Quality

The PTO Qwen serving evidence now separates durable implementation gaps from
data-acquisition gaps. Shard placement is proven by local file presence and
size records for all five shards. The metadata probe then validates actual
safetensors headers against the config-derived expected contract before any
CUDA allocation or device-weight binding is claimed.

The scaffold now reports shard placement and actual metadata validation as
passing, while keeping `qwen_weight_loader` partial until validated tensors are
bound to CUDA buffers and persistent-device task arguments.

## Evaluation Run

The shard download used the generated Hugging Face resolve URLs and wrote a
transfer log under `tmp/cuda-backend/pto-serving-shards-a16851f6/download.log`.

```bash
PYTHONPATH=$PWD:$PWD/python \
  .venv/bin/python examples/cuda/qwen_safetensors_fetch.py \
  --output-json \
  tmp/cuda-backend/pto-serving-shards-a16851f6/qwen-safetensors-shards.json
```

Result: `status=ready_for_metadata_probe`, `expected_shard_count=5`,
`present_shard_count=5`, `missing_shard_count=0`, and
`expected_tensor_count=399`.

```bash
PYTHONPATH=$PWD:$PWD/python \
  .venv/bin/python examples/cuda/qwen_safetensors_metadata.py \
  --weight-inventory-json \
  tmp/cuda-backend/pto-serving-weights-e06636e9/qwen-weight-inventory.json \
  --output-json \
  tmp/cuda-backend/pto-serving-safetensors-a16851f6/qwen-safetensors-metadata.json
```

Result: `status=metadata_validated`, `expected_shard_count=5`,
`opened_shard_count=5`, `missing_shard_count=0`,
`expected_tensor_count=399`, `validated_tensor_count=399`, and
`mismatch_count=0`.

## Remaining Gaps

- Bind the validated Qwen tensors to CUDA device buffers and
  persistent-device task arguments.
- Bind runtime token IDs, allocate and bind the real KV-cache buffers, generate
  Qwen kernel bodies, execute the decode loop, and import full-serving viewer
  rows for `Qwen/Qwen3-8B`.
- Keep the large raw safetensors shards under `tmp/`; do not commit them.
